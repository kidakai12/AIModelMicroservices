-- Optional seed data for Supabase local (runs on `supabase db reset`)
-- User-service tables are created by Alembic in the FastAPI container.

-- Example:
-- insert into public.profiles (id, name) values (...);
